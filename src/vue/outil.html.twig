{% extends "base.html.twig" %}

{% block title %} {{ parent() }} - Gestion des outils {% endblock %}
       
{% block contenu %} <h1> Gestion des outils </h1> 

{% if form.valide is defined %} 
    {% if form.valide == true %}
       <h1 class="h3 mb-3 font-weight-normal">{{ form.message }}</h1>
       {% else %}  
       <h1 class="alert alert-danger" role="alert">{{ form.message }}</h1> 
    {% endif %}         
{% endif %}         
    
<div class="panel">
  <!-- Default panel contents -->
  <div class="panel-heading background-violet">Liste des outils - <a href="index.php?page=outilajout" class="blanc">Ajouter un outil</a></div>

  <!-- Table -->
  <div class="table-responsive">
  <table class="table table-hover table-striped">
      <tr><th>Libellé</th><th>Version</th><th></th><th></th></tr>
   {% for e in liste %}
          <tr><td>{{e.libelle}}</td><td>{{e.version}}</td><td> <div class="btn-group btn-group-lg"><a href="index.php?page=outilmodif&id={{e.id}}"><span class="glyphicon glyphicon-pencil violet" aria-hidden="true"></span></a></div></td><td><div class="btn-group btn-group-lg"><a href="index.php?page=outil&id={{e.id}}"><span class="glyphicon glyphicon-trash violet" aria-hidden="true"></span></a></div></td></tr> 
   {% endfor %}    
  </table>
  </div>
</div>

{% endblock %}